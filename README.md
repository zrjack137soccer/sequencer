# Sequencer

Simple sequential function builder that allows for building a list of functions that will then be run in a sequence updating an object that represents a state

## Quickstart
Make sure lua is built and installed

Run:
```bash
lua sequencer.lua
```

sequencer.lua is currently the main file to execute the lua code for sequential function calling. All functions just defined within the file as well at the moment.
